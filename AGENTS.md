# AGENTS.md —— 仓库级协作约束

> 本文件写给在本仓库工作的模型 / 协作者（含各 Agent），与人类开发者同等适用。
> 历史教训：`docs/guides/permission-rules.md` 曾把本机真实的 Obsidian 知识库路径
> 写进示例（`D:\Notes\<真实库名>`），属于隐私事故，已在提交前修正并 amend。

## 红线一：仓库内容不得出现本机 / 个人真实信息

在**任何文档、示例、配置样例、HTML 原型、提交说明**中禁止出现：

- 本机真实路径：用户主目录（`C:\Users\<用户名>` 之类真实用户名）、个人目录
  （`D:\个人\...`、`D:\Work\...`、AppData 临时路径等实际磁盘布局）、本仓库绝对路径；
- IP / 内网地址 / 服务地址（`127.0.0.1`、`localhost` 回环除外）；
- 账号与身份：用户名、GitHub 句柄、邮箱、令牌、密钥、证书等凭据；
- 带个人属性的工具目录（如 `~\.confluence-mcp`、`~\.jenkins-mcp` 等）。

## 占位规范（写示例时的固定写法）

| 想表达 | 写法 |
|---|---|
| 用户主目录 | `C:\Users\<用户名>` 或 `%USERPROFILE%` |
| 本仓库的绝对位置 | `<repo>` |
| 示例数据目录 / 知识库 | 虚构且中性的路径（如 `D:\Notes\MyVault`、`D:\Shared\Tools`、`D:\Archive\202?`），**必须注明“示例，请替换为你的实际目录”** |
| 账号 / 邮箱示例 | `<用户名>`、`user@example.com` |

任何“看起来像真实配置”的样例都要先自问：这是不是从当前机器/会话抄来的？是就换占位。

## 提交前自查

提交或 amend 前，在仓库根执行下列扫描，**逐条人工确认命中项不含真实个人数据**后再提交
（允许项：`127.0.0.1`、`localhost`、`example.com`、`C:\Users\me`、`<用户名>`/`<repo>` 等占位符）：

```bash
git grep -n -i -E 'Xuledings|IDEA|Obsidian' -- '*.md' '*.html'
git grep -n -E 'C:[\\/]Users[\\/]|D:[\\/](Work|个人)|AppData|Xuledings' -- '*.md' '*.html'
git grep -n -E '[0-9]{1,3}(\.[0-9]{1,3}){3}|[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+' -- '*.md' '*.html'
```

命中即修改后再提交；只允许“占位 + 标注示例”的形态留存。

## 特例与边界

- **测试代码常量**（`test/*.mjs` 的 `WORKSPACE` 等）指向开发机/仓库自身，属运行必需：
  集中在 `test/` 与 `scripts/`，用途在文件头注释说明，**不写进任何示例文案或文档**；
  新增此类常量同样要最少化并注释。
- 真实环境交接/操作记录（含机器路径的命令）**不得入库**：放在本地忽略目录
  （如 `.local/`，已在 `.gitignore` 之外自行维护）或直接不提交；确需入库必须整体占位化。
- 修正文档中的敏感内容后，用 `git commit --amend` 覆盖“引入它的那一次提交”，
  避免个人路径残留在新历史里；若已推送，需 force-push（先与协作者确认）。

## 扩展建议（未实施）

把上面三行扫描固化成一个 `scripts/privacy-scan.mjs`（可配例外清单），接入
`npm test` 或 pre-commit，实现一键强制检查。
